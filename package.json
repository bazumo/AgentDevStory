{
  "name": "agentdevstory",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:gbrain": "bun run server/src/index.ts",
    "dev:all": "sh -c 'bun run dev:gbrain & server_pid=$!; npm run dev; kill $server_pid'",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.86.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
