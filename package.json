{
  "name": "agent-dev-story",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run dev:server & bun run dev:client",
    "dev:server": "bun run --cwd server dev",
    "dev:client": "bun run --cwd client dev",
    "build": "bun run --cwd client build",
    "typecheck": "bun run --cwd server typecheck"
  },
  "workspaces": [
    "server",
    "client"
  ]
}
